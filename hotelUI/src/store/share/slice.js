import { createSlice } from '@reduxjs/toolkit'

export const {actions: shareAction, reducer: shareReducer} = createSlice ({
    name: 'share',
    initialState: {
        loading: true
    },
    reducers: {
        
    },
    extraReducers: (builder) => {
        builder
        
    }
    
})